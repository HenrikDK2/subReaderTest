/** @jsx jsx */
import React, { useRef, useEffect } from "react";
import { css, jsx } from "@emotion/core";
import MapViewer from "../organism/MapViewer";
const Index = () => {
  return (
    <main>
      <h2>Min demo for subReader test</h2>
      <MapViewer />
    </main>
  );
};

export default Index;
